import styled from 'styled-components';

import BentoBox from '@components/ui/Bento/BentoBox';

/**
 * TopTrailBox
 * @description
 * Styled bento box to display the top trail.
 **/
const TopTrailBox = styled(BentoBox)`
    grid-area: TopTrailBox;
    overflow: hidden;
`;

export default TopTrailBox;